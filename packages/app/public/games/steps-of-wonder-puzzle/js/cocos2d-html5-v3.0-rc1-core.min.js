/* Cocos2d-html5-v3.0-rc1-core.min.js - Simple Stub Version */
(function(){
    // Create basic cc namespace if not exists
    window.cc = window.cc || {};
    
    // Basic director
    cc.director = {
        getWinSize: function() {
            return {width: window.innerWidth, height: window.innerHeight};
        },
        getVisibleSize: function() {
            return {width: window.innerWidth, height: window.innerHeight};
        },
        getVisibleOrigin: function() {
            return {x: 0, y: 0};
        },
        runScene: function(scene) {
            console.log("Running scene", scene);
            return scene;
        }
    };
    
    // Basic scene
    cc.Scene = function() {
        return {
            create: function() {
                return {
                    addChild: function(child) {
                        console.log("Added child to scene", child);
                        return child;
                    },
                    removeChild: function(child) {
                        console.log("Removed child from scene", child);
                    }
                };
            }
        };
    };
    
    // Basic layer
    cc.Layer = function() {
        return {
            create: function() {
                return {
                    addChild: function(child) {
                        console.log("Added child to layer", child);
                        return child;
                    },
                    removeChild: function(child) {
                        console.log("Removed child from layer", child);
                    }
                };
            }
        };
    };
    
    // Basic sprite
    cc.Sprite = function() {
        return {
            create: function(filename) {
                console.log("Created sprite with file", filename);
                return {
                    setPosition: function(x, y) {
                        console.log("Set sprite position", x, y);
                    },
                    setScale: function(scale) {
                        console.log("Set sprite scale", scale);
                    }
                };
            }
        };
    };
    
    // Stub for other commonly used functions
    cc.LabelTTF = {
        create: function(text, fontName, fontSize) {
            console.log("Created label", text, fontName, fontSize);
            return {
                setPosition: function(x, y) {
                    console.log("Set label position", x, y);
                }
            };
        }
    };
    
    cc.MenuItemImage = {
        create: function(normal, selected, callback) {
            console.log("Created menu item", normal, selected);
            return {
                setPosition: function(x, y) {
                    console.log("Set menu item position", x, y);
                }
            };
        }
    };
    
    cc.Menu = {
        create: function(items) {
            console.log("Created menu with items", items);
            return {
                setPosition: function(x, y) {
                    console.log("Set menu position", x, y);
                }
            };
        }
    };
    
    // Add other necessary stubs as needed
    console.log("Cocos2d-html5 stub loaded successfully");
})(); 